{
  "name": "acttus-os",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Acttus OS — painel de operações / calendário editorial (full-stack na Vercel).",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vercel dev",
    "db:setup": "node --env-file=.env.local scripts/db-setup.js",
    "key:create": "node --env-file=.env.local scripts/create-api-key.js"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.5",
    "@vercel/blob": "^2.4.0"
  }
}
